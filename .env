# ChromaDB path (required for storing large image embeddings)
CHROMADB_PATH=/Volumes/m01/kalliste_data/chromadb
# Any API keys if we add them later
# Environment-specific settings (dev/prod)
# Custom cache directories if we need to override defaults